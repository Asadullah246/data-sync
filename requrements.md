we have changed plan. now we need:
a simple node js ( you can use any additional library like express or any other) that will recive data from a connected postgress database and push it to another server via api call. the node js server. 
before sending, we need some verification. 

for now, just get the data from the postgress db and save those in a file(csv/json/any better format). 
a data should not be saved duplicate. 
the node js should pull data from the databse after every half hour. and insert the new data only in the file. 

are you got or have any questions ?